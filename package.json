{
  "name": "crooked",
  "description": "Next.js portfolio site with Sanity CMS, embedded Studio dashboards, job aggregation, geography seeding, and analytics.",
  "scripts": {
    "dev": "concurrently \"npm run dev:next\" \"npm run dev:studio\"",
    "dev:next": "npm run dev --workspace=frontend",
    "dev:studio": "npm run dev --workspace=studio",
    "build": "npm run build --workspace=frontend",
    "import-sample-data": "cd studio && sanity dataset import sample-data.tar.gz --replace"
  },
  "keywords": [
    "Sanity",
    "Next.js",
    "Headless CMS",
    "Visual Editing",
    "Live Content API (LCAPI)",
    "Real-time"
  ],
  "workspaces": [
    "studio",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1",
    "napi-postinstall": "^0.3.0",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@vercel/analytics": "^1.6.1"
  }
}
